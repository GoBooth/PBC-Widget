<!-- Tailwind CSS CDN -->
<script src="https://cdn.tailwindcss.com"></script>
<nav class="bg-gradient-to-r from-purple-900 via-indigo-900 to-black shadow-lg mb-6">
  <div class="container mx-auto px-4 py-4 flex justify-between items-center">
    <a href="/" class="text-2xl font-extrabold text-white tracking-wide animate-pulse">Photobooth Widget</a>
    <div>
    <% if (currentUser) { %>
      <span class="mr-4 text-gray-200">Hello, <%= currentUser.username %></span>
      <a href="/dashboard/widget" class="mr-4 px-3 py-1 text-gray-300 hover:text-white transition-colors rounded">Dashboard</a>
      <a href="/logout" class="px-3 py-1 bg-red-600 hover:bg-red-500 text-white rounded transition-colors">Logout</a>
    <% } else { %>
      <a href="/login" class="mr-4 px-3 py-1 text-gray-300 hover:text-white transition-colors rounded">Login</a>
      <a href="/register" class="px-3 py-1 text-gray-300 hover:text-white transition-colors rounded">Register</a>
    <% } %>
    </div>
  </div>
</nav>